@model PW3OrgTareas.ArchivoTarea

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Adjuntar un archivo</h1>
<br />
<div class="jumbotron">
    <div class="panel-body">
        <div class="col-md-9 col-md-offset-3">
            @using (Html.BeginForm("AdjuntarArchivo", "Tarea", FormMethod.Post, new { enctype = "multipart/form-data" }))
            {
                @Html.AntiForgeryToken()
                @Html.HiddenFor(x => x.IdTarea)
                <div class="form-horizontal">
                    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                    <div class="form-group">
                        @Html.LabelFor(model => model.RutaArchivo, "Archivo",htmlAttributes: new { @class = "control-label col-md-2" })
                        <div class="col-md-10">
                            @Html.TextBoxFor(model => model.RutaArchivo, new { type = "file" })
                        </div>
                    </div>

                    <div class="form-group">
                        <div class="col-md-offset-2 col-md-10">
                            <input type="submit" value="Crear adjunto" class="btn btn-success btn-block" />
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</div>